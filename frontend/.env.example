# Copy to .env: cp .env.example .env

# Backend REST API (include /api/v1 if your server uses that prefix)
VITE_API_URL=http://localhost:8000/api/v1

# Mapbox public token (safe to expose in the browser; still keep out of git via .env)
VITE_MAPBOX_TOKEN=pk.eyJ...
